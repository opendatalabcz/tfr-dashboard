name: tfr_dashboard
description: Dashboard providing visualization of TFR and other related datasets

publish_to: 'none'

version: 0.1.0

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_riverpod: ^1.0.3
  http: ^0.13.4
  shared_preferences: ^2.0.13
  syncfusion_flutter_charts: ^20.1.48
  url_launcher: ^6.0.20
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
